// Historical note: eval(userInput) was removed from the implementation.
// This comment is intentionally a false positive for the starter regex rule.
export function evaluateNumber(input: string) {
  return Number(input);
}
