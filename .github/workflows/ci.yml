name: Validate Traceward
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      NEXT_TELEMETRY_DISABLED: '1'
      LANGSMITH_TRACING: 'false'
      LANGCHAIN_TRACING_V2: 'false'
      TRACEWARD_AI: disabled
      TRACEWARD_SEMGREP: 'false'
      TRACEWARD_GITLEAKS: 'false'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'
      - name: Install dependencies
        shell: bash
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi
      - run: npm run format:check
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run benchmark
      - run: npm run test:graph
      - name: Generate local audit artifacts and exercise baseline gate
        run: |
          npm run cli -- audit fixtures/hardened-saas --format json --output traceward-ci-base.json
          npm run cli -- audit fixtures/hardened-saas --baseline traceward-ci-base.json --fail-on high --format sarif --output traceward-ci.sarif
      - uses: actions/upload-artifact@v4
        with:
          name: traceward-audit
          path: |
            traceward-ci-base.json
            traceward-ci.sarif
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
      - run: npm audit --audit-level=low
